# 2026-03-19

- User asked to maintain consistent note capture workflows in `cast42/notes` with immediate commit/push after note creation.
- Added multiple new notes and moved a Copilot-related note into `topics/magma/microsoft/copilot/`; user prefers Copilot content grouped under Copilot topic rather than AGI catch-all.
- User requested direct link lists for notes by topic (e.g., Microsoft Copilot / Copilot Studio) and expects precise GitHub paths (case-sensitive folders matter, e.g., `topics/AGI` vs `topics/agi`).
- Enabled Telegram-based exec approvals in OpenClaw config:
  - `channels.telegram.execApprovals.enabled = true`
  - `channels.telegram.execApprovals.approvers = ["8558246228"]`
  - `channels.telegram.execApprovals.target = "dm"`
- Elevated tool access configured for this user ID (`8558246228`) and frequently used with `/approve ...` flow.
- Sonos control progress: CLI installed and user Sonos devices discovered (`Bureau`, `Woonkamer`), but Apple Music playback automation from CLI blocked by SMAPI auth limitations/error; playback/status control works once content is already playing.
- User wants a maintained piano favorites list; created and pushed `refs/favorite_piano_albums.md` seeded with Dirk Maassen — *Eden (III) - The Rift*.
- qmd maintenance status refreshed: collection `cast42-notes` updated/embedded; current indexed corpus around 113 files with vectors embedded.
- Raindrop CLI is available and authenticated (`raindrop whoami` and `raindrop search` succeeded); command surface differs from assumptions (`collection` singular, no `raindrop list`).
